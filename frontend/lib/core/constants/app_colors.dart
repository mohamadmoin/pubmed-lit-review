// lib/core/constants/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2D7FF9);      // Bright Blue
  static const Color secondary = Color(0xFF18DAB6);    // Teal
  static const Color tertiary = Color(0xFFFFA26B);     // Coral

  // Neutral Colors
  static const Color background = Color(0xFFF8FAFC);   // Light Gray-Blue
  static const Color surface = Color(0xFFFFFFFF);      // White
  static const Color textPrimary = Color(0xFF1A1F36);  // Dark Blue-Gray
  static const Color textSecondary = Color(0xFF4F566B);// Medium Gray

  // Accent Colors
  static const Color accent1 = Color(0xFF00C7F4);      // Electric Blue
  static const Color accent2 = Color(0xFFFF6B6B);      // Soft Red
  static const Color accent3 = Color(0xFF7C4DFF);      // Purple

  // Feedback Colors
  static const Color success = Color(0xFF2DD4BF);      // Teal
  static const Color warning = Color(0xFFFBBF24);      // Yellow
  static const Color error = Color(0xFFFF6B6B);        // Red
  static const Color info = Color(0xFF60A5FA);         // Light Blue

  // Gradient Colors
  static const List<Color> primaryGradient = [
    Color(0xFF2D7FF9),
    Color(0xFF18DAB6),
  ];

  // Surface Colors
  static const Color surfaceHigh = Color(0xFFFFFFFF);
  static const Color surfaceMedium = Color(0xFFF1F5F9);
  static const Color surfaceLow = Color(0xFFE2E8F0);

  // Border Colors
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);
}
